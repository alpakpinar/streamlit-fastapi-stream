## Streamlit + FastAPI with Streaming

Very simple app with streaming enabled.

```
pip install -r requirements.txt

# Run frontend
streamlit run src/frontend.py

# Run backend
fastapi dev src/api.py
```
